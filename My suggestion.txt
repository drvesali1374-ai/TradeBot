مورد ۸ :

به نظر من این بخش تقریباً از پایه باید تغییر کند.


 اصلاً نیازی به این پیچیدگی نیست.

مستندات Toobit دقیقاً Endpoint زیر را برای همین کار ارائه کرده است:
GET /api/v1/futures/historyPositions
ویژگی‌های این Endpoint:

تمام پوزیشن‌های بسته شده را برمی‌گرداند.
اگر Symbol ارسال نشود، همه نمادها را برمی‌گرداند.
شامل اطلاعات Long و Short است.
شامل زمان باز و بسته شدن است.
شامل PnL است.
شامل قیمت ورود و خروج است.
امکان صفحه‌بندی تا 1000 رکورد را دارد.

بنابراین کل این قسمت:

حلقه روی automation_symbols

فراخوانی چندباره API

ادغام نتایج

کاملاً غیرضروری است.

من این بخش را به این شکل طراحی می‌کنم:
historyPositions

↓

عدم ارسال symbol

↓

دریافت تمام معاملات

↓

فیلتر فقط CLOSED

↓

SELL_CLOSE → LONG

BUY_CLOSE → SHORT

↓

Sort بر اساس closeTime DESC

↓

ذخیره در KV

↓

مقایسه با KV قبلی

↓

اگر تغییر داشت

↓

ارسال آخرین 10 رکورد به بله

این هم ساده‌تر است، هم سریع‌تر و هم تعداد درخواست‌های API را به حداقل می‌رساند.

۲- مورد ۵ (صفحه Login)

به نظر من این طراحی از نظر امنیت هنوز حرفه‌ای نیست.

 که Login را داخل Frontend قرار دهیم.

من این کار را انجام نمی‌دهم.

معماری پیشنهادی

حتی ساده‌تر

چون فقط خودم از پروژه استفاده می‌کنم، اصلاً JWT هم لازم نیست.

Cloudflare خودش این قابلیت را دارد که یک Session Cookie امضا شده تولید کند.

یعنی

Password

↓

Worker

↓

Set-Cookie

↓

Signed Cookie

↓

تمام

بدون دیتابیس

بدون JWT

بدون Refresh Token

بدون دردسر

برای پروژه شخصی کاملاً کافی است.

۳- قسمتی که درباره Cloudflare بود

به نظرم بیشترین دردسر مربوط به ذخیره تنظیمات و State است.

در پروژه تو الان KV وجود دارد.

من اصلاً چیزی را پیچیده نمی‌کنم.

پیشنهاد من:

KV

automationState

settings

lastHistoryPosition

lastSignal

lastNotification

lastRun

تمام.

نه Durable Object

نه D1

نه R2

نه Queue

فعلاً هیچ‌کدام لازم نیست.

KV برای این حجم داده کاملاً کافی است.

۴- پیشنهاد ساده‌تر برای Notification

به جای اینکه هر بار کل تاریخچه را بررسی کنیم.

من این کار را نمی‌کنم.

historyPositions

↓

اولین رکورد

↓

id

↓

با lastPositionId مقایسه

↓

اگر برابر بود

پایان

↓

اگر فرق داشت

↓

رکوردهای جدید

↓

ارسال به بله

↓

ذخیره lastPositionId

یعنی اصلاً نیازی نیست هر بار کل داده‌ها مقایسه شوند.

هم چنین حتما لازم است این مورد از درخواستم را :
"تعیین تعداد انجام چرخه در هر بار زدن دکمه "یک چرخه"، مثلا وقتی تعداد چرخه برابر 3 باشد باید وقتی دکمه "یک چرخه" ران میشود 3 بار چرخه پشت سر هم اجرا شود.این عدد باید در تنظیمات موجود در صفحه "اتوماسیون" قابل تنظیم باشد."
حتما و حتما در پروژه سمت کلودفلر هم اعمال شود .یعنی باید همه تغییراتی را که درخواست کرده بودم در کلودفلر هم حتما پیاد سازی شود. 

